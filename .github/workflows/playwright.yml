name: Playwright Tests + Allure + Slack (Final)

on:
  push:
    branches: [ main ]
  pull_request:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 🚀 Slack Start Notification
      - name: Slack Start Notification
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data "{
            \"text\": \"🚀 Playwright Started\nRepo: ${{ github.repository }}\nBranch: ${{ github.ref_name }}\nBy: ${{ github.actor }}\nCommit: ${{ github.sha }}\nMessage: ${{ github.event.head_commit.message }}\nTime: $(date)\"
          }" \
          ${{ secrets.SLACK_WEBHOOK_URL }}

      # 📥 Checkout
      - uses: actions/checkout@v4

      # 🟢 Setup Node
      - uses: actions/setup-node@v4
        with:
          node-version: 18

      # 📦 Install
      - name: Install Dependencies
        run: npm install

      # 🌐 Install Browsers
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # ▶️ Run Tests (IMPORTANT: never fail pipeline)
      - name: Run Playwright Tests
        run: npx playwright test || true

      # 🔍 Debug JSON (optional but useful)
      - name: Debug JSON
        run: cat results.json || echo "No results.json found"

      # 📊 Parse Results (FIXED)
      - name: Parse Test Results
        id: results
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

          if [ ! -f results.json ]; then
            echo "⚠️ results.json not found"
            echo "passed=0" >> $GITHUB_OUTPUT
            echo "failed=0" >> $GITHUB_OUTPUT
            echo "total=0" >> $GITHUB_OUTPUT
            exit 0
          fi

          PASSED=$(jq '[.. | objects | select(.status?=="passed")] | length' results.json)
          FAILED=$(jq '[.. | objects | select(.status?=="failed")] | length' results.json)
          TOTAL=$((PASSED + FAILED))

          echo "passed=$PASSED" >> $GITHUB_OUTPUT
          echo "failed=$FAILED" >> $GITHUB_OUTPUT
          echo "total=$TOTAL" >> $GITHUB_OUTPUT

      # 📊 Install Allure
      - name: Install Allure
        run: npm install -g allure-commandline --save-dev

      # 📊 Generate Allure Report
      - name: Generate Allure Report
        run: allure generate allure-results --clean || true

      # 🚀 Deploy Report
      - name: Deploy Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-report

      # 🔗 Report URL
      - name: Print Report URL
        run: echo "https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"

      # ✅ Slack Final Notification (ALWAYS RUNS)
      - name: Slack Final Notification
        if: always()
        run: |
          STATUS="✅ Passed"
          if [ "${{ steps.results.outputs.failed }}" != "0" ]; then
            STATUS="❌ Failed"
          fi

          curl -X POST -H 'Content-type: application/json' \
          --data "{
            \"blocks\": [
              {
                \"type\": \"header\",
                \"text\": { \"type\": \"plain_text\", \"text\": \"$STATUS Test Execution\" }
              },
              {
                \"type\": \"section\",
                \"fields\": [
                  { \"type\": \"mrkdwn\", \"text\": \"*Repo:*\n${{ github.repository }}\" },
                  { \"type\": \"mrkdwn\", \"text\": \"*Branch:*\n${{ github.ref_name }}\" },
                  { \"type\": \"mrkdwn\", \"text\": \"*By:*\n${{ github.actor }}\" }
                ]
              },
              {
                \"type\": \"section\",
                \"fields\": [
                  { \"type\": \"mrkdwn\", \"text\": \"*Total:*\n${{ steps.results.outputs.total }}\" },
                  { \"type\": \"mrkdwn\", \"text\": \"*Passed:*\n${{ steps.results.outputs.passed }}\" },
                  { \"type\": \"mrkdwn\", \"text\": \"*Failed:*\n${{ steps.results.outputs.failed }}\" }
                ]
              },
              {
                \"type\": \"section\",
                \"text\": {
                  \"type\": \"mrkdwn\",
                  \"text\": \"*Commit:* https://github.com/${{ github.repository }}/commit/${{ github.sha }}\"
                }
              },
              {
                \"type\": \"actions\",
                \"elements\": [
                  {
                    \"type\": \"button\",
                    \"text\": { \"type\": \"plain_text\", \"text\": \"View Report\" },
                    \"url\": \"https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/\"
                  },
                  {
                    \"type\": \"button\",
                    \"text\": { \"type\": \"plain_text\", \"text\": \"View Logs\" },
                    \"url\": \"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
                  }
                ]
              }
            ]
          }" \
          ${{ secrets.SLACK_WEBHOOK_URL }}